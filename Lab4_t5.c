#include<stdio.h>
int main ()
{
	int hours=24; int minutes=hours*60; int seconds= hours*3600;
	printf("time in minutes= %d",minutes);
	printf("\n time in seconds= %d", seconds );
	return 0;
}
//Zubair Ali CS-1c; Program to convert time from hours into minutes,seconds and days